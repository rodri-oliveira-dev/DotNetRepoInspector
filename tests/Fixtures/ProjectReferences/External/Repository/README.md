# External project reference

Treat this `Repository` directory as the inspection root. `App` references the sibling `Shared` project outside that root, proving external references are retained without becoming failures.
