# Invalid project fixture

Contains intentionally malformed project XML. The inspector should be able to identify/report a non-evaluable project without treating the malformed XML as a valid project model.
