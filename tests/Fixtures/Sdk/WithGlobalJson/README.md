# SDK fixture with global.json

Proves repository-local SDK configuration with:

- `version=10.0.100`
- `rollForward=latestFeature`
- `allowPrerelease=false`

The configured SDK version must remain distinct from whichever compatible SDK the environment actually resolves.
